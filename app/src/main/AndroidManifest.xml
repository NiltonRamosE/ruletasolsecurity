<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@drawable/sol_security_logo_2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/sol_security_logo_2"
        android:supportsRtl="true"
        android:theme="@style/Theme.RuletaSolSecurity"
        tools:targetApi="31">

        <!-- Pantalla principal: launcher de la app -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pantalla del juego (ruleta interactiva) - se implementará después -->
        <activity
            android:name=".GameActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

    </application>

</manifest>